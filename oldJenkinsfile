node {
    def app
     stage ('Clone'){
        checkout scm
     }
     stage ('Build images'){
        app = docker.build("tag1/httpd")
     }
     stage ('Test image'){
        withDockerContainer(image:'tag1/httpd')
            }
}
